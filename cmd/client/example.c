#include <stdio.h>
#include <stdlib.h>
int main (int argc, char** argv) {
  printf("Hello, I'm a program that prints a string to show that saving the last run job to a file is working correctly.\n");
  return EXIT_SUCCESS;
}
